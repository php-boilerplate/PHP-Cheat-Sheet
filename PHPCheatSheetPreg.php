<?php


/**
 * PHP Cheat Sheet - Preg
 * https://www.php.net/manual/en/ref.pcre.php
 */
class PHPCheatSheetPreg {
    function TableOfContent() {
        /**
         * Top functions:
         *   67.31% @see self::preg_match
         *   60.27% @see self::preg_replace
         *   40.55% @see self::preg_match_all
         *   36.98% @see self::preg_split
         *
         *
         * @see self::preg_filter — Perform a regular expression search and replace
         * @see self::preg_grep — Return array entries that match the pattern
         * @see self::preg_last_error_msg — Returns the error message of the last PCRE regex execution
         * @see self::preg_last_error — Returns the error code of the last PCRE regex execution
         * @see self::preg_match_all — Perform a global regular expression match
         * @see self::preg_match — Perform a regular expression match
         * @see self::preg_quote — Quote regular expression characters
         * @see self::preg_replace_callback_array — Perform a regular expression search and replace using callbacks
         * @see self::preg_replace_callback — Perform a regular expression search and replace using a callback
         * @see self::preg_replace — Perform a regular expression search and replace
         * @see self::preg_split — Split string by a regular expression
         */
    }

    function preg_filter() {}
    function preg_grep() {}
    function preg_last_error_msg() {}
    function preg_last_error() {}
    function preg_match_all() {}
    function preg_match() {}
    function preg_quote() {}
    function preg_replace_callback_array() {}
    function preg_replace_callback() {}
    function preg_replace() {}
    function preg_split() {}
}
