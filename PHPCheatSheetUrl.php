<?php


/**
 * PHP Cheat Sheet - Url
 * https://www.php.net/manual/en/ref.url.php
 */
class PHPCheatSheetUrl {
    function TableOfContent() {
        /**
         * Top functions:
         *   37.90% @see self::parse_url
         *   37.39% @see self::base64_encode
         *   33.30% @see self::urlencode
         *
         * @see self::base64_decode — Decodes data encoded with MIME base64
         * @see self::base64_encode — Encodes data with MIME base64
         * @see self::get_headers — Fetches all the headers sent by the server in response to an HTTP request
         * @see self::get_meta_tags — Extracts all meta tag content attributes from a file and returns an array
         * @see self::http_build_query — Generate URL-encoded query string
         * @see self::parse_url — Parse a URL and return its components
         * @see self::rawurldecode — Decode URL-encoded strings
         * @see self::rawurlencode — URL-encode according to RFC 3986
         * @see self::urldecode — Decodes URL-encoded string
         * @see self::urlencode — URL-encodes string
         */
    }

    function base64_decode() {}
    function base64_encode() {}
    function get_headers() {}
    function get_meta_tags() {}
    function http_build_query() {}
    function parse_url() {}
    function rawurldecode() {}
    function rawurlencode() {}
    function urldecode() {}
    function urlencode() {}
}
